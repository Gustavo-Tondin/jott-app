{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the Jott window needs on desktop. Kept minimal on purpose: the app only reaches the filesystem through its own Rust commands, so no blanket fs permission is granted to the webview. Desktop-only because the window commands below (dragging, minimize, maximize) describe a window the user can move — mobile has no such thing, and asking for them there fails the build.",
  "platforms": ["linux", "windows", "macOS"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-internal-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "core:window:allow-is-maximized"
  ]
}
